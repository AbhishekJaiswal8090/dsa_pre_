#include <iostream>
#include <list>
#include <algorithm>
using namespace std;

int main()
{
    std::list<int> ll;
    ll.push_back(10);
    ll.push_front(23);
    ll.push_back(45);
    ll.push_front(7);
    ll.pop_front();
    cout < < val < < endl;
}
